'use client'
// import { useLanguage } from '@/context/LanguageContext'
import React, { Fragment } from 'react'

export default function Loading() {
	// const { loading } = useLanguage()
	return (
		<Fragment>
		{/* {loading && (
			<div className='absolute w-full h-screen flex items-center justify-center bg-black top-0 z-50'>
				Loding...ds
			</div>
		)} */}
		</Fragment>
	
	)
}
